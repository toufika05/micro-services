server:
  port: 9296
  
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/toufika05/micro-services
          default-label: main
          clone-on-start: true  
 
